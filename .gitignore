# Dependencies
node_modules/
frontend/node_modules/
backend/venv/
backend/.venv/
__pycache__/
*.pyc

# Build output
frontend/dist/

# Data (persistent on Railway volume, not in git)
backend/data/

# Environment secrets
.env
backend/.env

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Temporary
*.tmp
*.log

# Debug / one-off scripts at root
debug_hours.py
fix_photos*.py
test_ocr.py
report_debug.json
sunday_report_raw.json

# Large binary assets at root (not source code)
*.jpg
*.png
*.pdf
*.zip
[EXTERNAL]*/

# Android build outputs
frontend/android/.gradle/
frontend/android/app/build/
frontend/android/build/
frontend/android/capacitor-cordova-android-plugins/build/

# Desktop build output (PyInstaller)
desktop/dist/
desktop/build/

# Gemini agent state
.gemini/

# Railway CLI local link state (project/service IDs, per machine)
.railway/
frontend/android/build_old/

# Staged front end for the desktop bundle — a copy of frontend/dist, produced
# at build time. Committing it would be a second copy of the build output.
desktop/web/

# Installer output — 14 MB build artifact, rebuilt by ISCC from the .iss
desktop/installer/*.exe
