# Workspace Rules — Daily Reporter V3

## Deployment
- Terry does NOT use git. Do NOT run git commands (add, commit, push, etc.).
- For deployments, use the **Railway CLI** (`railway up`) directly — it uploads from the local filesystem without git.
