# Production environment — used by Capacitor mobile builds and Railway.
# WHY: In dev, Vite proxies /api → localhost:8000.
# In the Capacitor APK, there is no local server — calls must go to Railway directly.
VITE_API_URL=https://residentengineerassistant-production.up.railway.app
