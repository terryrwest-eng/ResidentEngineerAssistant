# Android build outputs — do not sync to OneDrive or commit to Git
.gradle/
build/
app/build/
*.apk
*.aab
local.properties
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/

# Release signing. The keystore and its passwords never go in the repo:
# anyone with both can ship an update that Android accepts as this app.
release.keystore
keystore.properties
